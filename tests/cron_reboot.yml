cron:
  period: 1
  jobs:
    - type: bash
      script:
        echo "A" >> cron_reboot.out
    - skip:  2
      delay: 1
      type: reboot
    - type: bash
      script:
        echo "B" >> cron_reboot.out
